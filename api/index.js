export default () => {
  // TODO: catch all route that pulls from app folder
  // 1. if match in app/backend/components, use that route
  // 2. if match anywhere else in app, serve that file
  // 2.1. process file as needed
  // 3. if no match, return 404

  return "Hello, World!";
};
