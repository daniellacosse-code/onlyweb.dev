{
  "engines": {
    "node": "16.x"
  },
  "name": "only-web",
  "packageManager": "yarn@3.2.4",
  "private": true,
  "scripts": {
    "build:all": "yarn workspaces foreach -pivt run build",
    "watch:all": "yarn workspaces foreach -piv run watch",
    "_nuxt": "yarn workspace @only-web/nuxt"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
